version: "3.9"

services:
  nextjs:
    container_name: nextjs-bun-app
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
    restart: unless-stopped
